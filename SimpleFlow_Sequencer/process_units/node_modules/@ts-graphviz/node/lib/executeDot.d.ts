/// <reference types="node" />
import { ExecuteOption } from './types';
/**
 * A low-level API for wrappers for dot commands provided by Graphviz.
 */
export declare function executeDot(dot: string, { format, output, suppressWarnings, dotCommand: cmd, childProcessOptions }?: ExecuteOption): Promise<Buffer>;
