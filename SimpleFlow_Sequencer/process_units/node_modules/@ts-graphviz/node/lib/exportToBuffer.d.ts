/// <reference types="node" />
import { IRootCluster } from 'ts-graphviz';
import { ExecuteOption } from './types';
declare type Option = Omit<ExecuteOption, 'output'>;
/**
 * Returns the Graphviz output result as a buffer.
 */
export declare function exportToBuffer(dot: IRootCluster | string, options?: Option): Promise<Buffer>;
export {};
