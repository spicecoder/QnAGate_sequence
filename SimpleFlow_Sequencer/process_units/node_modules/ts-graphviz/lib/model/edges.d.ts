import { EdgeTarget, EdgeAttributes, IAttributes, EdgeTargets } from '../types';
import { DotObject } from './abstract';
import { attribute } from '../attribute';
/**
 * @category Primary
 */
export declare class Edge extends DotObject {
    readonly targets: ReadonlyArray<EdgeTarget | EdgeTargets>;
    /** Comments to include when outputting with toDot. */
    comment?: string;
    readonly attributes: IAttributes<attribute.Edge>;
    constructor(targets: ReadonlyArray<EdgeTarget | EdgeTargets>, attributes?: EdgeAttributes);
}
